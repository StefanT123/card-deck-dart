# Card Deck app written in Dart

### This is just a simple deck card written in dart language. This application is written for learning purposes.

#### To start the app in docker, run `docker-compose run --rm app`
